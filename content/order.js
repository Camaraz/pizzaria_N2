import { StyleSheet } from "react-native";

const orderStyles = StyleSheet.create({
    
    roundButton: {
        width: 30,
        height: 30,
        borderRadius: 15
    }
});

export default orderStyles;